const mongose = require("mongose");
const Student = mongose.model("Student");

const createStudentObject = (req) => {}

// Operação de criação (Create)
module.exports.createStudent = async (req, res) => {}